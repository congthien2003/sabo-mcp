{
	"name": "memorize-mcp",
	"version": "1.2.1",
	"description": "A minimal Model Context Protocol (MCP) server to persist work summaries as local JSON memories.",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"dev": "bun run index.ts",
		"start": "bun run index.ts",
		"lint": "echo \"No linter configured yet\""
	},
	"devDependencies": {
		"@types/bun": "latest",
		"@types/node": "^25.0.3"
	},
	"peerDependencies": {
		"typescript": "^5.9.3"
	},
	"dependencies": {
		"@modelcontextprotocol/sdk": "^1.25.1",
		"@supabase/supabase-js": "^2.89.0"
	}
}
